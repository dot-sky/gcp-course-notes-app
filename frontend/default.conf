server {
    listen 8080; # Cloud Run expects your app to listen on the PORT env var, typically 8080
    root /usr/share/nginx/html; # This is where your static files will be copied
    index index.html index.htm;

    location / {
        try_files $uri $uri/ /index.html; # Essential for Single Page Applications (SPAs)
    }
}